using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PushTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     Debug.Log("プッシュとマージが成功しました");
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
